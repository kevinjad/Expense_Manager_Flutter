import 'package:flutter/material.dart';

class SetAlert extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Set Alert',
        style: TextStyle(
          fontFamily: 'Quicksand',
        ),
        ),
      ),
      body: Container(),
    );
  }
}